# 三高産業 ハウスEC(プロトタイプ)

ビニールハウス・パイプハウスの資材販売と施工のためのECサイト試作版です。
中心は **ハウスカルテ**(お客様ごとのハウス台帳)と **3Dハウスシミュレーター** で、
「担当者が訪問時にハウスを登録 → お客様はスマホで自分のハウスを見る → 張り替え・修理・新設の見積りをその場で依頼」
という流れを一通り動かせます。

サーバー不要の静的サイトなので、このリポジトリの GitHub Pages でそのまま公開できます。
公開URLの例: `https://<ユーザー名>.github.io/<リポジトリ名>/mitaka-ec/`

## ページ構成

| ファイル | 誰が使う | 内容 |
|---|---|---|
| `index.html` | お客様 | トップ(特徴・ハウスカルテの案内・取り扱い資材・流れ・FAQ) |
| `karte.html` | お客様 | **マイハウスカルテ**。`?c=お客様コード` で開く。圃場ごとの地図・全棟の仕様・張り替え目安・工事記録・「張り替えを見積る」 |
| `simulator.html` | お客様・担当者 | 3Dハウスシミュレーター + 概算見積り(URLに内容を保存) |
| `product.html` | お客様 | **商品1点のページ**(`?id=品番`)。寸法図と「使う場所」3D、税込価格と入数・1個あたり、数量、かご、電話・写真相談、一緒に使うもの、施工班のひとこと、カルテとの適合表示、スマホ下部の固定購入バー |
| `catalog.html` | お客様 | **資材をさがす**。困りごとタイル、6つの棚(骨組み・張る・風を通す・あたためる冷やす・水をやる・まもる)、ひらがな/声で検索、写真の代わりの寸法図、詳細シート、かご |
| `library.html` | お客様 | **みんなのハウス図鑑**。名前・場所を出さない集計(5棟未満は非表示)と作物別の定番構成 |
| `quote.html` | お客様 | 見積依頼(シミュレーター内容・見積リスト・カルテのハウスを添えて送信) |
| `staff.html` | 担当者 | **ハウスカルテ登録**。お客様・圃場・ハウスを登録し、カルテURLのQRコードを発行。一覧・JSON書き出し |
| `partners.html` | 事業者 | 資材店・施工店・メーカー向けの出店・パートナー募集 |
| `privacy.html` | お客様 | 登録情報の取り扱い(同意文面の下書き) |

## 仕組み

| ファイル | 役割 |
|---|---|
| `assets/config.js` | **設定はここだけ。** Supabase接続情報、LINE友だち追加URL、見積送信先、担当者コード |
| `assets/store.js` | カルテのデータ層。既定はブラウザ保存(localStorage)、Supabaseの接続情報を入れると本番DBに保存。デモデータもここ |
| `assets/pricing.js` | **数量計算と概算見積りのロジック。単価表(`UNIT`)と選択肢(`OPTIONS`)はここ。** 張り替えのみの概算(`estimateRecover`)も含む |
| `assets/house3d.js` | three.js によるハウスの3D描画 |
| `assets/simulator.js` / `karte.js` / `staff.js` / `library.js` / `catalog.js` / `quote.js` | 各ページの動作 |
| `assets/icons.js` / `figures.js` | 写真が無い商品を「形のアイコン + 大きな規格文字」の寸法図で見せる |
| `assets/voice.js` | 声で探す(対応ブラウザのみボタン表示) |
| `assets/product.js` / `product.css` / `product-data.js` | 商品ページと、その派生情報(光らせる部位、一緒に使うもの、適合、施工班のひとこと) |
| `assets/index.js` / `index.css` | トップページ(3Dヒーロー、時刻スライダー、スクロール連動カメラ、節気の仕事) |
| `assets/scene.js` / `sekki.js` | 風景SVG(赤城山・田畑)と二十四節気の表 |
| `assets/catalog-data.js` | カタログ商品データ(佐藤産業・誠和・東都興業・汎用)、6つの棚 `SHELVES`、困りごと `PURPOSES`、ふりがな `KANA`。`// BEGIN PRODUCTS` 〜 `// END PRODUCTS` はスクリプトで書き換える範囲 |
| `assets/site.js` / `style.css` / `simulator.css` / `catalog.css` | 共通UI(かご、文字サイズ3段階)とデザイン |

## デザイン方針「暁(あかつき)」

ベンチマーク(Bootstrap Farmer、Johnny's Selected Seeds、Greenhouse Megastore、食べチョク、坂ノ途中、北欧暮らしの道具店、Snow Peak、Aesop、土屋鞄、Tesla/IKEA/meviy の構成体験)を調べたうえで、次の方針で作っています。

- 色は「深緑黒 #0B1F16 × 朝焼け #E8622A」の2色対比。緑を面に塗らない。ダーク面は感情(ヒーロー・カルテ・フッター)、明るい面は**純白の「図面の紙」**(極薄の方眼を地にだけ敷き、カードは白。方眼の有無と1pxの線だけで面の差をつける)。ベージュは使わない。棚の6色は面でなく線(左バー・見出し文字)に使う。
- 書体は見出し Zen Old Mincho、本文 BIZ UDPゴシック(UD設計)。数字は等幅。
- 写真は使わず、実時間3D(`house3d.js`)と線画SVGで画をつくる。トップのヒーローは3Dのハウスに朝6時の低い光を当て、**時刻スライダー**で太陽・空・影が動く。スクロールで外観→軒下→内部へカメラが寄る。
- 角丸は 4 / 12 / 18 / 999 の4段、明るい面では影を使わず1pxの線。動きは 160/240/420/900ms。
- 農業らしさは、朝の光、赤城山の稜線(面の境界の罫線)、二十四節気の帯(最上部)、土色の下線だけで出す。
- 70代向けは「文字を大きく」だけに頼らず、行間1.75、コントラスト4.5:1、タップ56px、農家の言葉(「被覆資材」→「張る」)で対応。

カタログの設計方針は「絵で選ぶ資材棚」です。商品より先に注意書きを置かない、品番はカード表面に出さない、文字は15px以上・ボタンは56px以上、13分類は6つの棚に束ねる、数量は −/+ で入力させない、を守っています。
| `scripts/import-catalog.mjs` | 販売管理の商品マスタ(CSV)から `catalog-data.js` の商品を生成 |
| `supabase/schema.sql` | 本番DBのテーブル定義(customers / plots / houses)と図鑑用ビュー |
| `vendor/three/`, `vendor/qrcode/` | three.js r180、QRコード生成(いずれもMIT)。CDNに依存せず動くよう同梱 |

## 使い方(プロトタイプ)

1. `staff.html` を開き、担当者コード(初期値 `1234`、`config.js` で変更)で開く。
2. 「デモデータを入れる」を押すと、お客様3名・ハウス6棟のサンプルが入る。`karte.html?c=DEMO01` で見られる。
3. 訪問先では、お客様 → 圃場(現在地をピン) → ハウス(複数棟は「この棟を追加」) → 同意 → 「登録してカルテを発行」。
4. 発行されたQRコードを読み取る、またはURLをLINEで送ると、お客様が自分のカルテを開ける。
5. カルテの「張り替えを見積る」「修理・相談」「3Dで見る」から、見積依頼やシミュレーターにつながる。

ブラウザ保存モードでは、登録した端末のブラウザにだけデータが残ります(社内テスト用)。
複数の担当者・お客様で共有するには次の「本番運用の前にやること」の 1 を行います。

## 本番運用の前にやること

1. **データベースをつなぐ**: Supabase でプロジェクトを作り、`supabase/schema.sql` を実行し、URLと anon キーを `config.js` に入れる。行レベルセキュリティは schema.sql の注記どおり本番方針に変更する(担当者はログイン必須、お客様は自分のコードの分だけ閲覧)。
2. **LINEをつなぐ**: LINE公式アカウントの友だち追加URLを `config.js` に入れる。お知らせ配信(張り替え時期・災害前点検)は Messaging API で後から追加。
3. **単価を差し替える**: `pricing.js` の `UNIT` と `OPTIONS.films[].perSqm` を自社単価に。`PRICING_VERSION` も更新。
4. **商品マスタを取り込む**: 販売管理から CSV を出し、`node mitaka-ec/scripts/import-catalog.mjs 商品マスタ.csv`。列の形式は `scripts/sample-products.csv` を参照。約2万点でも動くが、商品数が数千を超えたら検索とページ送りの改善を行う。
5. **見積依頼の送信先**: `config.js` の `mailTo` と、用意できれば `quoteEndpoint`。
6. **会社情報・同意文面**: フッター、`quote.html` の連絡先、`privacy.html` の文面を確定する。
7. **公開設定**: 各ページの `<meta name="robots" content="noindex">` と「プロトタイプ版」バナーを外す。

## メーカー品のデータについて

佐藤産業・誠和・東都興業の商品は、公開されている商品名・用途をもとに「雰囲気を出すため」に入れたものです。
規格と価格は確認前の参考値(または要見積)なので、正式運用では商品マスタからの取り込みに置き換えてください。

## 概算見積りの計算方法(概要)

- アーチは「垂直の脚 + 半楕円」でモデル化し、弧長をラマヌジャンの近似式で求めています。
- アーチ本数 = 奥行 ÷ アーチ間隔 + 1。母屋の本数は間口で 3・5・7 通りに切り替わります。
- 被覆材面積は屋根・側面・両妻面の合計に 10% のロスを加えています。
- 施工費は床面積 × m²単価 + 基本料。張り替えのみは被覆面積 × 単価 + 基本料 + 旧フィルム処分。
- 運搬費は地域で固定額(その他地域は別途見積り)。消費税は 10%。

## 今後の拡張候補

- 注文・決済(掛け払いの請求書発行、カード決済)と注文履歴のカルテ反映
- LINE通知(張り替え時期、台風・降雪前の点検、災害時の優先受付)
- 補助金申請用の見積書・仕様書PDFの自動作成(3D画像付き)
- 出店者(他社)ごとの商品・単価設定、JAとの連携
- 写真からの部材特定(LINEで写真を送ると担当者が見積りを返す)

## 動作確認

```bash
npx serve mitaka-ec   # または python3 -m http.server -d mitaka-ec 8000
```

ブラウザで `http://localhost:3000/staff.html` を開き、担当者コード `1234` で入ります。3D表示には WebGL が必要です。
